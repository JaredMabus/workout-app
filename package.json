{
  "name": "workout-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd dist && NODE_ENV=production node server.js",
    "start-pm2": "npx pm2 start pm2.config.js --no-daemon",
    "build": "cd server && npm run build && cd ../client && npm run build",
    "build-client": "cd client && npm run build",
    "prod-install": "npm install && cd dist && npm install",
    "server-install": "cd server && npm install",
    "server-watch": "cd server && npm run watch",
    "seed": "cd server && npm run seed"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/JaredMabus/workout-app.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/JaredMabus/workout-app/issues"
  },
  "homepage": "https://github.com/JaredMabus/workout-app#readme",
  "dependencies": {
    "concurrently": "^7.5.0",
    "pm2": "^5.3.0"
  },
  "devDependencies": {
    "typescript": "^4.9.3"
  }
}
