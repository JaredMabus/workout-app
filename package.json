{
  "name": "workout-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx pm2 start pm2.config.js --no-daemon",
    "build": "cd server && npm run build && cd ../client && npm run build",
    "build-client": "cd client && npm run build",
    "install-build": "npm install && cd server && npm install && cd ../client && npm install",
    "install-prod": "cd dist && npm install",
    "server-install": "cd server && npm install",
    "client-install": "cd client && npm install --legacy-peer-deps",
    "watch": "cd server && npm run watch",
    "client": "cd client && npm start",
    "seed": "cd server && npm run seed"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/JaredMabus/workout-app.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/JaredMabus/workout-app/issues"
  },
  "homepage": "https://github.com/JaredMabus/workout-app#readme",
  "dependencies": {
    "concurrently": "^7.5.0",
    "pm2": "^5.3.0",
    "vm2": ">=3.9.18"
  },
  "devDependencies": {
    "typescript": "^4.9.3",
    "vm2": ">=3.9.18"
  }
}
